#include "Framework.h"
#include "ShotGun.h"

ShotGun::ShotGun(string file)
	:Gun(file)
{
	//for (int i = 0; i < 20; i++)
	//{
	//	Bullet* bullet = new Bullet();
	//	bullet->SetTarget(this);

	//	bullets.push_back(bullet);
	//}
}

ShotGun::~ShotGun()
{
}